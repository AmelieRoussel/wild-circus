<nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top align-items-center">
    <a class="navbar-brand text-light site-title" href="{{ path("home") }}">
        <img src="{{ asset('build/images/juggler.png') }}" alt="logo" class="logo mr-3">
        Wild Circus
    </a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbar" aria-controls="navbar"
            aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse justify-content-end" id="navbar">
        <ul class="navbar-nav mt-4 mb-2 my-lg-0">
            <li class="nav-item ml-3">
                <a class="nav-link text-light" href="/#aboutUs">Notre cirque</a>
            </li>
            <li class="nav-item ml-3">
                <a class="nav-link text-light" href="{{ path("performance") }}">Performances</a>
            </li>
            {% if is_granted("ROLE_USER") %}
                <li class="nav-link ml-3 dropdown">
                    <a class="dropdown-toggle text-light text-decoration-none" href="#" role="button"
                       id="dropdownMenu"
                       data-toggle="dropdown"
                       aria-haspopup="true" aria-expanded="false">
                        L'école
                    </a>
                    <div class="dropdown-menu bg-dark" aria-labelledby="dropdownMenu">
                        <a class="dropdown-item" href="{{ path("school_home") }}">Présentation</a>
                        <a class="dropdown-item" href="{{ path("school_enrollment") }}">Inscription</a>
                    </div>
                </li>
            {% else %}
                <li class="nav-item ml-3">
                    <a class="nav-link text-light" href="{{ path("school_home") }}">L'école</a>
                </li>
            {% endif %}
            <li class="nav-item ml-3">
                <a class="nav-link text-light" href="{{ path("news") }}">Actualités</a>
            </li>
            <li class="nav-item ml-3">
                <a class="nav-link text-light" href="#">Tarifs</a>
            </li>
            <li class="nav-item ml-3">
                <a class="nav-link text-light" href="{{ path("contact") }}">Contact</a>
            </li>
            {% if is_granted("ROLE_ADMIN") %}
                <li class="nav-item ml-3">
                    <a class="nav-link text-light" href="{{ path("admin_home") }}">Administration</a>
                </li>
            {% endif %}
            <li class="nav-item ml-3">
                {% if app.user %}
                    <a class="nav-link text-light" href="{{ path('app_logout') }}">Se déconnecter</a>
                {% else %}
                    <a class="nav-link text-light" href="{{ path('app_login') }}">Se connecter</a>
                {% endif %}
            </li>
        </ul>
    </div>
</nav>