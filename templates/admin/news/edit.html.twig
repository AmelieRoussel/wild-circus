{% extends 'layoutAdmin.html.twig' %}

{% block title %}{{ parent() }} | Modifier une actualité{% endblock %}
{% block body %}
    <h1 class="text-center mt-4">Modifier une actualité</h1>
    <div class="container">
        {{ include('admin/news/_form.html.twig', {'button_label': 'Modifier'}) }}
        <div class="row mx-0 d-flex justify-content-center delete">
            {{ include('admin/news/_delete_form.html.twig', {'button_label': 'Supprimer'}) }}
        </div>
        <div class="row mx-0">
            <a class="back text-decoration-none btn rounded-pill my-4 px-3 py-1"
               href="{{ path('admin_news_index') }}">Retour</a>
        </div>
    </div>
{% endblock %}
