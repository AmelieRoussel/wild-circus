{% extends 'layoutAdmin.html.twig' %}

{% block title %}{{ parent() }} | Ajouter un créneau horaire{% endblock %}
{% block body %}
    <h1 class="text-center mt-4">Ajouter un créneau horaire</h1>
    <div class="new container ">
        {{ include('admin/course/_form.html.twig') }}
        <div class="row mx-0">
            <a class="back text-decoration-none btn rounded-pill my-4 px-3 py-1"
               href="{{ path('admin_course_index') }}">Retour</a>
        </div>
    </div>
{% endblock %}
