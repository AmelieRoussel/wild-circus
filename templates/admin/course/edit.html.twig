{% extends 'layoutAdmin.html.twig' %}

{% block title %}{{ parent() }} | Modifier un créneau horaire{% endblock %}
{% block body %}
    <h1 class="text-center mt-4">Modifier un créneau horaire</h1>
    <div class="container">
        {{ include('admin/course/_form.html.twig', {'button_label': 'Modifier'}) }}
        <div class="row mx-0 d-flex justify-content-center delete">
            {{ include('admin/course/_delete_form.html.twig', {'button_label': 'Supprimer'}) }}
        </div>
        <div class="row mx-0">
            <a class="back text-decoration-none btn rounded-pill my-4 px-3 py-1"
               href="{{ path('admin_course_index') }}">Retour</a>
        </div>
    </div>
{% endblock %}
